apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: car-marketplace
  namespace: car-marketplace
  annotations:
    kubernetes.io/ingress.class: "traefik"          # Civo K3s uses Traefik by default
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    traefik.ingress.kubernetes.io/router.entrypoints: "websecure"
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  tls:
    - hosts:
        - cars.127.0.0.1.nip.io           # ← replace with your domain or Civo external IP
      secretName: car-marketplace-tls
  rules:
    - host: cars.127.0.0.1.nip.io         # ← same as above
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: car-marketplace
                port:
                  name: http
